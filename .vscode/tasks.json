{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "task: runClientDebugWSL",
      "type": "shell",
      "command": "powershell.exe -Command \"& {./gradlew.bat runClient --debug-jvm}\"",
      "detail": "Runs the client ON WINDOWS (via PowerShell) and waits for a debugger on port 5005."
    },
    {
      "label": "task: runClientDebugWin",
      "type": "shell",
      "command": ".\\gradlew.bat runClient --debug-jvm",
      "detail": "Runs the client ON WINDOWS (via PowerShell) and waits for a debugger on port 5005."
    }
  ]
}